<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Detail</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        /* Additional Styles */
        .order-info {
            margin-bottom: 20px;
        }

        .product-image {
            max-width: 100px;
        }
    </style>
</head>

<body>
     <!-- Navbar -->
     <nav class="navbar navbar-expand-lg navbar-light bg-dark fixed-top p-3">
        <a style="color: #0F0;" class="navbar-brand" href=""><b><em>Fonekart</em></b></a>
    </nav>
    <div class="container mb-5" style="margin-top: 90px; border: rgba(0, 0, 0, 0.632) solid 1px; ">
        <div class="row mt-4">
            <div class="col-md-8 mx-auto ">
                <h2 class="text-center text-danger">Order Details</h2>
                <div class="order-info ">
                    Id :<span>
                        <%= order._id %>
                    </span>
                </div>
                <div class="order-info">
                    <h4 class="text-primary">Product</h4>
                    <div class="row">
                      <div class="col-md-3">
                        <img src="<%= order.orderItems[0].Image[0] %>" alt="Product Image" class="img-fluid product-image">
                      </div>
                      <div class="col-md-9">
                        <p><strong>Product Name:</strong> <%= order.orderItems[0].Pname %></p>
                        <p><strong>Quantity:</strong> <%= order.orderItems[0].quantity %></p>
                        <p><strong>Price:</strong> <%=order.orderItems[0].quantity * order.orderItems[0].price %></p>
                        <p><strong>Status:</strong> <%= order.orderItems[0].orderStatus %></p>
                      </div>
                    </div>
                  </div>
                <div class="order-info">
                    <h4 class="text-primary">Delivery Address</h4>
                    <p><strong>Name:</strong>
                        <%= order.address.name %>
                    </p>
                    <p><strong>Address:</strong>
                        <%= order.address.address %>
                    </p>
                    <p><strong>Phone:</strong>
                        <%= order.address.phone %>
                    </p>
                    <p><strong>Postal Code:</strong>
                        <%= order.address.pin %>
                    </p>
                </div>
                <div class="order-info">
                    <h4 class="text-primary">Payment Method</h4>
                    <p><strong>Method:</strong>
                        <%= order.paymentMethod %>
                    </p>
                </div>
                <div class="order-info">
                    <h4 class="text-primary">Order Information</h4>
                    <p><strong>Order Date:</strong>
                        <%= new Date(order.orderDate).toLocaleDateString() %>
                    </p>
                    <p><strong>Total Price:</strong>
                        <%=order.orderItems[0].quantity * order.orderItems[0].price %>
                    </p>
                </div>

            </div>
        </div>
    </div>
</body>

</html>